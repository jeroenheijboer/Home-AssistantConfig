 - alias: Send notification on open of door
   trigger:
    platform: state
    entity_id:
    - binary_sensor.door_window_sensor_158d0001a1ecd5
    # voeg hier meer sensoren toe
    to: 'open'
   action:
    service: notify.ios_iphone_van_martin
    data_template:
     message: >
       {{ trigger.to_state.attributes.friendly_name }} is {{ trigger.to_state.state }}